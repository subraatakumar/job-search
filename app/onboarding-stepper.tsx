import Link from "next/link";
const steps=[{label:"Profile",href:"/profile"},{label:"Job search",href:"/search"},{label:"AI provider",href:"/settings"},{label:"First opportunity",href:"#jobs"}];
export default function OnboardingStepper({current,completed}:{current:number;completed:number}){return <nav className="onboarding-stepper" aria-label="Onboarding progress">{steps.map((step,index)=><div className="onboarding-step-wrap" key={step.label}><Link className={`onboarding-step${index===current?" step-current":""}${index<completed?" step-done":""}`} href={step.href}><span className="step-svg">{index<completed?<svg viewBox="0 0 32 32" aria-hidden="true"><circle cx="16" cy="16" r="14"/><path d="m9 16 5 5 9-10"/></svg>:<svg viewBox="0 0 32 32" aria-hidden="true"><rect x="5" y="5" width="22" height="22" rx="9"/></svg>}</span><span>{step.label}</span></Link>{index<steps.length-1&&<span className={`step-connector${index<completed?" connector-done":""}`} aria-hidden="true"><svg viewBox="0 0 70 12"><path d="M2 6h58m0 0-6-5m6 5-6 5"/></svg></span>}</div>)}</nav>}
