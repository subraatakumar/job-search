"use client"; import {useEffect,useState} from "react"; import {useRouter} from "next/navigation";
export default function AiProviderForm(){const router=useRouter();const[provider,setProvider]=useState("ollama");const[endpoint,setEndpoint]=useState("http://host.docker.internal:11434/v1");const[model,setModel]=useState("llama3.2");const[apiKey,setApiKey]=useState("");const[saved,setSaved]=useState(false);const[status,setStatus]=useState("");const[error,setError]=useState("");useEffect(()=>{fetch("/api/ai-provider").then(r=>r.json()).then(data=>{if(data?.provider){setProvider(data.provider);setEndpoint(data.endpoint);setModel(data.model);setStatus(data.has_api_key?"A saved API key is encrypted on the server.":"No API key saved.")}})},[]);async function save(){setError("");const response=await fetch("/api/ai-provider",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({provider,endpoint,model,apiKey})});const result=await response.json();if(!response.ok)return setError(result.error??"Could not save provider.");setSaved(true);setStatus("Saved securely on the server.");setApiKey("")}async function test(){setError("");setStatus("Testing connection…");const response=await fetch("/api/ai-provider",{method:"PUT"});const result=await response.json();setStatus(response.ok?result.message:"");if(!response.ok)setError(result.error??"Connection failed.")}return <section className="card upload-card"><div className="review-grid"><label>Provider<select value={provider} onChange={e=>{setProvider(e.target.value);setEndpoint(e.target.value==="ollama"?"http://host.docker.internal:11434/v1":"https://api.openai.com/v1")}}><option value="ollama">Ollama (local)</option><option value="openai">OpenAI</option><option value="compatible">Other OpenAI-compatible endpoint</option></select></label><label>Model name<input value={model} onChange={e=>setModel(e.target.value)}/></label><label className="full-field">API endpoint<input value={endpoint} onChange={e=>setEndpoint(e.target.value)}/></label><label className="full-field">API key <span className="muted">leave blank to keep existing key</span><input type="password" value={apiKey} placeholder="Never displayed after saving" onChange={e=>setApiKey(e.target.value)}/></label></div><div className="privacy-note">🔒 API keys are encrypted with AES-256-GCM before storage and are never returned to the browser.</div><div className="review-actions"><button className="primary-button" type="button" onClick={save}>{saved?"Saved securely ✓":"Save provider →"}</button><button className="secondary-button" type="button" onClick={test}>Test connection</button></div>{status&&<p className="success-message">{status}</p>}{error&&<p className="error-message">{error}</p>}</section>}
