import {headers} from "next/headers";import{redirect}from"next/navigation";import{readSession}from"@/lib/session";import{getJobs,getJobSources}from"@/lib/job-source-repository";import SourceSearchActions from"./source-search-actions";
export default async function JobsPage(){const session=readSession(new Request("http://localhost",{headers:await headers()}));if(!session)redirect("/api/auth/login");const[jobs,sources]=await Promise.all([getJobs(session.id),getJobSources(session.id)]);return <main><nav className="topbar"><a href="/dashboard">JobSearch</a><a href="/dashboard">Dashboard</a></nav><div className="eyebrow">Job opportunities</div><h1>Review your opportunities</h1><p>Search your saved public sources and review listings before running resume matching.</p><SourceSearchActions sources={sources}/>{jobs.length?<section className="job-list">{jobs.map(job=><article className="card job-card" key={job.id}><div className="job-card-header"><div><span className="review-badge">Needs review</span><h2>{job.title}</h2><p className="job-meta">{job.company}{job.location?` · ${job.location}`:""}</p></div><a className="text-link" href={job.source_url} target="_blank" rel="noreferrer">View source ↗</a></div><p className="job-description">{job.description}</p><small className="muted">First seen {new Date(job.first_seen_at).toLocaleDateString()}</small></article>)}</section>:<section className="card empty-card"><div className="empty-illustration">⌕</div><div className="section-label">No jobs yet</div><h2>Search a saved source to begin</h2><p>Use Search now above, or add another public source if you want to expand your discovery.</p><a className="secondary-button" href="/sources">Manage sources →</a></section>}</main>}
